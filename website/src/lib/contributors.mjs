// Real human contributors to ZackEyes, sourced from the GitHub contributors
// graph: https://github.com/yangshiqi/ZackEyes/graphs/contributors
//
// BOTS ARE EXCLUDED ON PURPOSE. AI co-authors (Claude, Codex, Gemini) only
// ever appear as `Co-Authored-By` trailers — never as the commit *author* —
// so the GitHub contributors API does not list them. The remaining bot,
// `github-actions[bot]`, plus any future `*[bot]` / `*-bot` account, is left
// out here by hand. Add a row when a new human lands commits (check the graph
// and skip anything whose GitHub account `type` is `Bot`).
export const contributors = [
  { login: 'yangshiqi', name: 'yangshiqi' },
  { login: 'Foen1x', name: 'Johnny Fu' },
];
