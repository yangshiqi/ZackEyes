# Semantic Color System Implementation Plan

1. Add centralized SwiftUI and AppKit semantic tokens with focused color-value tests.
2. Route usage thresholds and menu-bar traffic-light colors through the shared tokens.
3. Change Overlap to steel blue and add a clamped 1px endpoint with geometry tests.
4. Replace duplicated functional colors across Notch, SimulatedNotch, Settings, and utility windows.
5. Leave decorative Buddy/F1 palettes untouched.
6. Update architecture, build, test, assemble, and visually verify the affected surfaces.
